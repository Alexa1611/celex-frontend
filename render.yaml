services:
  - type: web
    name: celex-frontend
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist/angular-frases/browser
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: NODE_VERSION
        value: "20"
